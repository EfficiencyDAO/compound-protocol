import {
    init,
} from './utils'

describe("Accrue Interest Tests | Efficiency Protocol", () => {
    before(async () => {
        await init()
    })

    describe('validate accrue interest', () => {

    })
})
