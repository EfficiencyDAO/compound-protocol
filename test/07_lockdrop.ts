import {
    init,
} from './utils'

describe("Lockdrop Tests | Efficiency Protocol", () => {
    before(async () => {
        await init()
    })

    describe('validate user balance', () => {

    })

    describe('set lock duration', () => {

    })

    describe('attempt claim', () => {

    })

    describe('validate reward', () => {

    })
})
